# FLY-010 – Monorepo bootstrap

## Session 1 · 2026-08-28 · completed

**State:** in-review. Branch `feat/FLY-010-monorepo-bootstrap`, stacked on
`docs/phase-1-foundation`.

**Done:**

- pnpm workspace with `catalog:` pins from ADR 0001 (Phase 1 re-verification).
- Root `package.json` (`packageManager: pnpm@11.24.0`, `engines.node >= 20.9.0`).
- Turborepo pipelines; package skeletons for `@flyte/{aviation,aviation-data,db,ui,config}`
  and `web`.
- `scripts/backlog-sync.mjs` regenerates `docs/BACKLOG.md` from frontmatter. Idempotent.
- `.env.example` no longer mentions Lane A / Lane B.
- `pnpm install --frozen-lockfile` succeeds. `pnpm exec tsc --version` is **6.0.3**.
- `pnpm verify` is green (skeleton no-op scripts in each package). Turbo warns that `build`
  produced no output files; expected until FLY-013.

**Not done:** real typecheck/lint/test (FLY-011). Next.js (FLY-013).

**Decisions made, do not re-litigate:**

- Catalog lists every ADR 0001 package now, even unused ones. One place to bump.
- Skeleton scripts are `node -e "process.exit(0)"` so they are portable on Windows.

**Watch out:**

- The local pnpm 11.24.0 copy under `%LOCALAPPDATA%\pnpm\.tools\@pnpm+win-x64\11.24.0` is
  incomplete (`dist/pnpm.mjs` missing). `npm exec --yes pnpm@11.24.0` works. If `pnpm`
  fails to switch after this lands, reinstall: `npm install -g pnpm@11.24.0`.
- Do not run `pnpm add typescript` without the catalog pin; npm latest is 7.0.2.
- `strict-peer-dependencies=true` in `.npmrc` makes `npm` print a warning. It is a pnpm
  setting. Leave it; do not delete it to silence npm.

**Next concrete step:** FLY-011 (shared ESLint / tsconfig / Vitest / CI), after this PR is
reviewed and merged.
