# FLY-015 – UI kit, glassmorphism, theme, night mode

## Session 1 · 2026-08-29 · completed

**State:** in-review. Branch `feat/FLY-015-ui-theme-night`, stacked on FLY-014
(`feat/FLY-014-better-auth`). Merge PR #10 first, then this PR onto `main`.

**Done:**

- Owner decision recorded as [ADR 0014](../adr/0014-glassmorphism.md): glassmorphism is the
  visual language for the whole application (Apple-like, large radius, translucent chrome).
  Night is red glass, more opaque. Sunlight: fills stay higher than marketing glass.
  `prefers-reduced-transparency` disables blur.
- [ADR 0013](../adr/0013-shadcn-ui.md): copy-in kit in `packages/ui`. CLI zinc theme is not
  the product. No new catalog packages this PR (cva / tailwind-merge / Radix wait until a
  component needs them).
- `@flyte/ui` exports `Button`, `Input`, `Label`, `ThemeProvider`, `ThemeSwitch`.
- Tokens in `packages/ui/src/styles.css`. Web imports `@flyte/ui/styles.css`.
- Themes `light` / `dark` / `night` via `data-theme` on `<html>`. Default `dark`.
  Persistence `localStorage` key `flyte-theme`. Inline script avoids a white flash.
- Theme switch in the shared chrome (login and home). Copy in `messages/{pl,en}/theme.json`.
- Login, home and top bar use the glass material. Auth forms use `@flyte/ui` controls.
- Unit test: `applyTheme("night")` writes `data-theme`. No jsdom in the catalog, so no RTL
  click test of `ThemeSwitch` this PR.
- `pnpm verify` green (this session).
- AGENTS.md and CONTRIBUTING.md point at ADR 0014 so later agents do not undo it.

**Not done:** Tailwind 4 utilities inside components; shadcn CLI add of further primitives;
accent/transparency/radius sliders (Phase 8 binds the tokens). Print templates stay
unglassed.

**Decisions made, do not re-litigate:**

- Default theme is dark so a cockpit does not flash white before storage is read.
- Glass fill is deliberately denser than Dribbble glassmorphism. Opacity is a token for
  Phase 8, not a second design.
- Theme labels are passed into `ThemeSwitch` so `@flyte/ui` does not import next-intl.

**Watch out:**

- `next.config.ts` must `transpilePackages: ["@flyte/ui"]`.
- Do not commit `apps/web/next-env.d.ts` if `next dev` injects `.next/types` imports.
- Extra paths outside the original owns_paths: `AGENTS.md`, `docs/CONTRIBUTING.md`,
  `(auth)` login forms, `app-chrome`, locale layout, `i18n/request.ts`. Required to apply
  the owner rule on every current screen.

**Next concrete step:** after merge, look at 375 px and cycle light / dark / night on
`/pl/login`. Then FLY-016 (PWA) or FLY-018 (units).

## Session 2 · 2026-08-29 · stopped for handoff

**State:** in-review. Branch `feat/FLY-015-ui-theme-night`, pushed. PR #11 retargeted
onto current `main` (FLY-014 landed as squash #10; the FLY-014 commit was dropped from
this branch with `rebase --onto origin/main 352c300`).

**Done this session:**

- Owner asked for email-first login and a form that fills the glass panel.
  Commit `feat(web): email-first login that fills the glass panel`.
- First screen is email only. `POST /api/login/email-status` looks up
  `user.email` (case-insensitive) and then either password sign-in or registration.
  Google, passkey and magic link sit below after the email step.
- `.auth-forms` is full width of `.panel`; `.shell` is a column that fills `100dvh`.
- Owner is reviewing the live page and will send more visual notes.

**Not done:**

- Incoming agent **reviews PR #11 first** (HANDOFF.md §4). Do not start FLY-016/018
  until #11 is merged, unless the owner says otherwise.
- Owner visual review of glass + login flow may still produce change requests.
  Wait for those before calling FLY-015 done.
- Tailwind 4 utilities / shadcn CLI extras still out of scope for this PR.
- Passkey on Windows Hello still not exercised in this environment.

**Decisions made, do not re-litigate:**

- Email enumeration (exists vs not) is an explicit owner UX request, not an accident.
  Do not "fix" it by collapsing both paths into one ambiguous form.
- `/api/login/email-status` is a sibling of `/api/auth/*` on purpose. A route under
  `/api/auth/` would be swallowed by Better Auth's `[...all]` catch-all.

**Rejected:** putting the existence check on `/api/auth/email-status`. Next.js would
hand it to Better Auth.

**Watch out:**

- Same stacking trap as #3/#7/#10: squash-merge onto `main`, then rebase the next
  branch with `--onto origin/main <parent-of-feature-commits>`. Do not open a PR
  whose base is an already-merged promote branch.
- Do not commit `apps/web/next-env.d.ts` when `next dev` adds `.next/types` imports.
- `pnpm --filter web dev` may already have been stopped. Restart it for UI review.

**Next concrete step for the incoming agent:**

1. `git fetch` and check out `feat/FLY-015-ui-theme-night`.
2. Review https://github.com/czekanskyy/flyte/pull/11 against CONTRIBUTING.md.
   Approve or request changes. Do not rubber-stamp glass contrast in night mode.
3. If the owner has new UI notes, implement them on this branch.
4. After #11 merges: FLY-018 (units, can start after FLY-011, already on main) or
   FLY-016 (PWA). FLY-019 still waits for FLY-015 + FLY-014.
