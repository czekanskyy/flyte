# FLY-019 – First-run acknowledgement, credits, app chrome

## Session 1 · 2026-08-29 · implementation complete

**State:** in-review. Branch `feat/FLY-019-first-run-ack`. FLY-018 is already on `main`.
Owner has logged in locally.

**Done:**

- 1:1 table `safety_acknowledgement` (`user_id`, `version`, `acknowledged_at`). Not
  columns on Better Auth `user`. Migration `0002_fly019_safety_acknowledgement.sql`
  applied to Neon **dev**.
- Version string `safety-1.1`. Bump it to re-prompt. Gate helper
  `planningDestination` sends unsigned-ack users to `/acknowledge`, never `/plan`.
- On-screen PL/EN statements match SAFETY.md §1.1 character for character (tested).
- `/plan` placeholder behind the gate. `/credits` authenticated stub: OpenAIP is
  **not** loaded, PANSA AIS linked, printouts have no attribution. ADR 0009 written.
- App chrome (signed-in): Home, Plan, Credits, existing theme + language + sign out.
- Tests: 9 in web (`safety-ack.test.ts`) plus schema coverage. `pnpm verify` green.

**Not done:** exercising the accept button in a headed browser this session. The
owner is already signed in; `/pl/plan` should redirect to `/pl/acknowledge` until
they accept.

**Decisions made, do not re-litigate:**

- 1:1 table, not `user` columns. FLY-012 said application fields stay off Better
  Auth tables.
- `/credits` stays inside `(app)` (D-004 default). Not public.
- Nav Plan/Credits only on the authenticated chrome (`appLinks`), not on `/login`.
- Drizzle named the SQL `0002_wide_mandrill.sql`; renamed to
  `0002_fly019_safety_acknowledgement.sql` and the journal tag matched.

**Rejected:** putting acknowledgement columns on `user`. Next agent will see the
Better Auth table and want to add two fields. Do not.

**Watch out:**

- `proxy.ts` is cookie-only. The real ack check is in `plan/page.tsx`. Do not
  treat a 200 on `/plan` from a session cookie as "ack is done".
- `drizzle-kit generate` invents Marvel names. Rename before commit.
- `packages/db/migrations/meta/0002_snapshot.json` must be Prettier-formatted or
  `pnpm lint` fails.
- Do not commit `apps/web/next-env.d.ts` when `next dev` / `next build` injects
  `.next/types` imports.

**Next concrete step:** incoming agent reviews the PR against SAFETY.md §1.1 and
DATA_SOURCES.md §10. After merge: FLY-016 (PWA) or FLY-017 (Docker/CD).
