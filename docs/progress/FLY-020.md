# FLY-020 – Wind triangle, angle wrap and leg ETE

## Session 1 · 2026-08-29 · implementation complete

**State:** in-review. Branch `feat/FLY-020-wind-triangle`. FLY-017 leftover docs are
stashed on `main` (`wip(docs): FLY-017 local compose verify notes...`), not in this PR.

**Done:**

- DOMAIN.md §6.5: same-frame substitution. Citation is planar rotation invariance of
  §6.2. No §3.2 in this slice.
- IMPLEMENTATION_PLAN.md fast-track note: logbook paused; FLY-020 then FLY-021.
- Task files FLY-020 / FLY-021. `pnpm backlog:sync`.
- `normaliseAngle` / `signedDelta` in radians. Range (−π, π]; exactly 180° is positive.
- `solveWindTriangle` (§6.2) with `{ ok: true, wca, heading, gs } | { ok: false,
  reason: "no-solution" }`. Asin domain or GS ≤ 0, TAS ≤ 0, WS < 0.
- `legEteSeconds`, `sumDurations`, `displayMinutesCeil` (§7).
- Golden vectors `packages/aviation/test/vectors/navigation.json` including
  `wind-triangle-001` expects unchanged. 92 aviation tests.

**Not done:** `/plan` UI (FLY-021). TrueNAS. Logbook.

**Decisions made, do not re-litigate:**

- Course and wind share one frame. Mixing MT with true wind is out of scope.
- WS = 0 short-circuits to WCA = +0 and GS = TAS so `asin(-0)` cannot leak.
- 100 NM / 100 kt ETE expect stays 3600 s; IEEE remainder is covered by tolerance 1e-9,
  not by changing the expect.

**Watch out:**

- JS `%` is not Euclidean. Use the helper in `geo/angles.ts`.
- `expect(wca).toBe(0)` fails on `-0`. Use `toBeCloseTo` or a short-circuit.
- FLY-021 must not land in this PR.

**Next concrete step:** incoming agent reviews this PR against DOMAIN.md §6. After merge,
FLY-021 on `feat/FLY-021-manual-ofp-plan`.
