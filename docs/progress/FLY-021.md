# FLY-021 – Manual /plan table

## Session 1 · 2026-08-29 · implementation complete

**State:** in-review. Branch `feat/FLY-021-manual-ofp-plan`, stacked on
`feat/FLY-020-wind-triangle` (PR #16). Do not merge this before #16.

**Done:**

- `/plan` still gated by session + safety ack. Placeholder copy replaced with a live
  table: shared TAS / wind from / wind speed, DIST and MT per leg.
- `computeManualOfp` converts NM/kt/deg at the UI boundary and calls FLY-020. Tests:
  `wind-triangle-001` tolerance 0.05; 2.5 NM + 2.5 NM at 100 kt TAS, zero wind → 3 min
  total not 4.
- PL/EN in `messages/*/plan.json`. Same-frame warning. Stated as a calculator, not an
  OFP snapshot.
- `@flyte/aviation` workspace dep; `transpilePackages` includes it.

**Not done:** print, ETO, fuel, snapshots, map, weather, VAR. Browser pass at 375 px was
not run (no browser tool in this session); CSS uses `--touch` 44 px and horizontal scroll
on the table.

**Watch out:**

- Merge order: #16 first, then retarget this PR to `main`.
- Import path from `plan/manual-ofp-form.tsx` is four levels to `src/lib`.
- jsdom is not in the catalog; do not add it to make a RTL test. Behaviour is in
  `compute-manual-ofp.test.ts`.

**Next concrete step:** review after #16 is on `main`. Owner can type legs on `/plan`.
