# FLY-017 – Docker Compose and CD to GHCR

## Session 1 · 2026-08-29 · implementation complete

**State:** in-review. Branch `feat/FLY-017-docker-cd`. FLY-016 is on `main`.

**Done:**

- `output: "standalone"` and `outputFileTracingRoot` at the repo root in
  `apps/web/next.config.ts`. Local `next build --webpack` produces
  `apps/web/.next/standalone/apps/web/server.js`.
- Multi-stage `Dockerfile`, non-root `flyte` (uid 1001), no secrets in the
  image. Healthcheck hits `/api/health`.
- `docker-compose.yml` (local build) and `docker-compose.prod.yml` (GHCR pull):
  `flyte-web`, `flyte-redis`, `flyte-pdf` stub, optional `cloudflared` profile.
- PDF stub: `/health` 200, any other path 501. Exercised with Node on
  localhost:3001 this session.
- `.github/workflows/cd.yml` pushes `ghcr.io/czekanskyy/flyte-web` on `main`
  (sha + `latest`) and on `v*` tags. `GITHUB_TOKEN`, `packages: write`. Does
  not deploy to TrueNAS.
- `OWNER_SETUP.md` "After GHCR has a flyte-web image": pull, compose up, tunnel
  already in §3.
- `pnpm verify` green. Docker is not part of that gate.

**Not done:** `docker compose up` on this machine. Docker Compose CLI v5.3.1 is
installed; the engine is not running (`dockerDesktopLinuxEngine` pipe missing).
Did not start Docker Desktop. Owner should `docker compose build flyte-web` once
the daemon is up.

**Decisions made, do not re-litigate:**

- Prod compose is a complete file, not a `!reset` overlay. TrueNAS Compose may
  be older than the reset merge syntax.
- CD publishes **web only**. The PDF stub is built from `docker/pdf-stub` on the
  NAS (or locally). Do not invent a second GHCR image this PR.
- Alpine + `wget` for the web healthcheck so the runner stays tiny.

**Watch out:**

- Daemon was down, so the image was not built here. First `docker compose build`
  is the real test of pnpm + argon2 on Alpine.
- `outputFileTracingRoot` must stay the repo root or standalone omits workspace
  packages.
- Do not put the Neon **main** `DATABASE_URL` in `.env.local`. Compose reads it
  from the environment / a NAS env file.

**Next concrete step:** incoming agent reviews the PR. After merge, Phase 1
code is complete; the owner pulls GHCR on TrueNAS and points the tunnel.
